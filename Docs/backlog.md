<!--
Как читать этот файл:
* [ ] Название — зачем/проблема в 1 строку. [done-when: критерий готовности]
Секции сверху вниз = по убыванию приоритета. «Сделано» внизу чистим, когда суть уехала в spec.md/decisions.md.
-->

# Backlog

## 🎯 Сейчас / следующее (P1)

* [x] Исправить публикацию неотвеченного вопроса с пустым ответом — кнопка «Опубликовать» на `/admin/qna` переводит запись в `active` даже если поле «Ответ» не заполнено; должна быть ошибка вместо тихой публикации пустого ответа. [done-when: API `POST /api/qna/[id]/publish` отклоняет публикацию без `answer` с понятным сообщением об ошибке, UI показывает её через toast]
* [x] Убрать колонку «Источник» из таблицы на `/admin/qna` (только фронтенд) — для основного сценария работы преподавателя она не нужна и загромождает таблицу. Поле `sourceDocument` в БД оставить как есть — может понадобиться в будущем (например, для группировки/фильтрации по исходному документу). [done-when: колонка не отображается в таблице `/admin/qna`, схема БД и API не меняются]
* [x] Растянуть таблицу на `/admin/qna` на всю ширину экрана — сейчас контейнер ограничен `max-w-7xl`, по бокам много пустого места, из-за этого колонки «Вопрос»/«Ответ» узкие и неудобно редактировать. [done-when: контейнер страницы использует всю доступную ширину (с разумными боковыми отступами), таблица заметно шире]
* [x] Добавить создание FAQ вручную — сейчас пары можно только импортировать (Excel/PDF/DOCX); нужна форма для ручного добавления одной пары. [done-when: на `/admin/qna` есть форма «Добавить вопрос-ответ», запись создаётся как `active` и индексируется в Qdrant]
* [x] Добавить индикатор обработки / индексации — загрузка PDF/DOCX идёт через LLM и долгая, пользователь не видит прогресс. [done-when: при upload показывается статус «обрабатывается / готово»]
* [x] Улучшить UX уведомлений — заменить текущие сообщения на toast-нотификации (успех/ошибка действий в админке). [done-when: действия (создание/правка/удаление/upload) дают неблокирующий toast]
* [x] Добавить bulk actions (checkbox + select all + delete) — массовые операции над списком QnA, чтобы не удалять записи по одной. [done-when: чекбоксы в строках + «выбрать все» + массовое удаление (soft-delete)]
* [x] Сортировка таблицы `/admin/qna` по колонкам «Вопрос», «Ответ», «Дата» — клик по заголовку переключает направление. [done-when: клик по заголовку сортирует список по возрастанию/убыванию, стрелка показывает текущее направление]
* [x] Лог ответов бота (успешных и NULL) — сейчас фиксируются только неотвеченные вопросы; успешные ответы нигде не сохраняются, и нет контроля, что «Судья» выбирает правильные ответы. Подход: отдельная анонимная таблица-лог (вопрос пользователя, топ-кандидаты со score, вердикт «Судьи», снапшот выданного ответа, timestamp). Просмотр — на **отдельной странице** `/admin/log` («Журнал»), не на `/admin/qna`: журнал — поток событий для чтения, база знаний — для редактирования; в шапке появляется навигация из двух пунктов. Фундамент для «Observability / debug panel» из P2. При реализации зафиксировать добавление таблицы в `decisions.md`. [done-when: каждое обращение к боту записано в лог и его можно посмотреть на `/admin/log`]
* [x] Русификация текстов интерфейса — убрать англицизмы из видимых пользователю надписей (частный случай для русскоязычных преподавателей; технические пути вроде `/admin/qna` не трогаем). Уже известные места: подзаголовок «Модерация и публикация вопросов QnA» на `/admin/qna`, название «QnA Bot AI» в шапке. [done-when: в UI админки не осталось англоязычных надписей, кроме неизбежных технических]
* [x] Довести redesign до консистентного состояния — редизайн делался через Lovable и переносился в Next.js; цель — визуальная консистентность при сохранении привычной навигации (заказчик просил не менять навигацию). [done-when: единый визуальный стиль на `/admin/qna`, агрессивные цвета убраны]

---

## 🔒 Перед публичным запуском — безопасность (P1.5)

> **Гейт:** не включать публичный / SaaS-режим, пока этот блок не закрыт.

### Core security / trust boundaries

* [x] Threat modeling (лёгкий, под масштаб пилота) — `Docs/threat-model.md`: активы, закрытые угрозы, принятые риски, шпаргалка реагирования (2026-07-05). Полноценный threat modeling для public SaaS — см. ниже.
* [ ] Threat modeling для public SaaS режима — пересмотр `threat-model.md` по триггерам из него (второй админ, несколько серверов, SaaS)
* [x] Продумать trust boundaries и tenant boundaries — trust: секция в `threat-model.md` (2026-07-05); tenant: N/A на пилоте, multi-tenant → P2
* [x] Secret separation (dev/staging/prod) — `.env.{env}.local` + шаблоны, `APP_ENV`, preload; см. `spec.md` §7.1, `decisions.md` 2026-07-04
* [x] Отдельный Telegram bot для production — `@VibeCodingFAQBot` создан, токен в `.env.production.local` (см. `decisions.md`, 2026-07-04)

### Upload / file handling security

* [x] Безопасная обработка upload файлов — magic bytes проверяются перед парсингом (`%PDF`, ZIP `PK\x03\x04`), несовпадение → 400
* [x] Ограничения размера файлов — 10 МБ в `upload/route.ts`
* [x] Ограничения MIME/type файлов — allowlist pdf/docx/xlsx (с фолбэком на расширение)
* [x] Upload isolation (non-public storage) — файлы не сохраняются на диск вовсе: буфер парсится в памяти и отбрасывается
* [x] Валидация Excel / CSV импорта — magic bytes + пустые/неполные строки пропускаются в `parseExcelQA`
* [x] Ограничение количества строк / размера импортов — макс. 500 пар за загрузку, вопрос ≤1000 символов, ответ ≤8000 (см. `spec.md` §7.2)

### Auth / abuse protection

* [x] Admin panel hardening — пароль без логина, подписанная сессия-cookie, `middleware.ts` защищает страницы и API (см. `decisions.md`, 2026-07-03)
* [x] Добавить авторизацию
* [x] Rate limiting — бот: 3 вопроса/мин на чат (in-memory, анонимность сохранена); логин: 5 неудач с IP → 15 мин блокировки (см. `spec.md` §3.2, §5.0; `decisions.md` 2026-07-05)
* [x] Basic audit logging — ошибки обработки сообщений бота пишутся в `bot_log` (verdict=`error`) и видны на `/admin/log` (см. `spec.md` §3.4)

### AI / tooling hygiene

* [x] Проверить Cursor / AI tooling boundaries — `.cursorignore` дополнен (`backups/`, `*.db.gz`); границы и Privacy Mode — `decisions.md` 2026-07-05
* [x] Разовый аудит dependency vulnerabilities — 3 high закрыты `npm audit fix`, 8 moderate приняты осознанно (см. `decisions.md` 2026-07-05)
* [ ] Регулярная проверка dependency vulnerabilities — повторять `npm audit` периодически (например, раз в месяц или при деплое). [done-when: определён и работает регулярный процесс]
* [x] Настроить basic backup strategy — `scripts/backup.sh` (горячий бэкап `logs.db`, ротация 7 шт.) + cron ежедневно в 03:00 + `scripts/reindex.ts` для восстановления Qdrant из SQLite (2026-07-05)
* [ ] Регулярный бэкап в холодное хранилище Таймвеб — сейчас архивы лежат только на диске самого сервера; при отказе диска пропадут вместе с базой. Нужно копировать `backups/logs-*.db.gz` в S3-совместимое холодное хранилище Timeweb Cloud (например, `rclone`/`s3cmd` по тому же cron). [done-when: свежий архив автоматически появляется в бакете Таймвеб, восстановление из него проверено]
* [x] Проверить production .env hygiene перед релизом — в git только шаблоны, секретов в истории нет, права 600 (2026-07-05)
* [x] Заполнить `TEACHER_CONTACT_URL` в `.env.production.local` — задано, все обязательные переменные prod на месте (проверено 2026-07-05)
* [ ] Первый реальный запуск production (`npm start`, `APP_ENV=production`) — `.env.production.local` подготовлен, бот `@VibeCodingFAQBot` создан; сам запуск ещё не выполнялся. [done-when: prod-бот отвечает студентам, админка работает под prod-паролем]

---

## 📋 Позже (P2)

### Мультипреподавательский режим

* [ ] Учётные записи преподавателей + суперадминка владельца — важная задача на перспективу нескольких преподавателей. Логин = имя пользователя Telegram, пароль генерирует владелец продукта вручную в суперадминской панели. До этого момента живём с одной общей админкой для преподавателя и владельца (простая авторизация — см. P1.5). [done-when: владелец создаёт/блокирует учётки преподавателей в суперадминке, преподаватель входит по логину/паролю]
* [ ] Многопользовательская работа: у каждого преподавателя своя база знаний и свой бот — менее приоритетно, чем учётки; сначала выкатить в прод однопользовательскую версию и проверить востребованность. Потребует tenant-идентификатор в схеме БД, раздельные коллекции Qdrant, несколько bot-токенов. Связано с «Multi-tenant» из P3. [done-when: два преподавателя работают независимо, данные и боты не пересекаются]

* [ ] Поддержка картинок — пожелание заказчика: прикреплять изображения к QnA-парам (вопрос/ответ с картинкой). [done-when: к паре можно прикрепить картинку, она хранится и отдаётся ботом/в админке]
* [ ] Observability / debug panel — панель для отладки RAG-пайплайна (что нашлось в Qdrant, что выбрал «Судья»).
* [ ] Улучшение readability / accessibility — в т.ч. управление размером шрифта; общая читаемость интерфейса.
* [ ] Спеки и архитектурная документация — постепенно фиксировать архитектурные/продуктовые решения и lifecycle (цель: сохранять контекст для будущего себя и ИИ-агентов). Частично закрыто: `spec.md` + `decisions.md`.
* [ ] Добавить soft duplicate detection — повторная загрузка FAQ сейчас создаёт новые записи (дубли). Выбранный подход: НЕ блокировать и НЕ авто-обновлять, а подсвечивать дубли и оставлять решение пользователю (см. `decisions.md`). [done-when: при импорте похожие записи помечаются как возможные дубли]
* [ ] Подсветка возможных дублей — UI-часть detection: визуально выделять подозрительные пары в списке.
* [ ] Продумать UX повторной загрузки FAQ — что делать, когда заливают тот же файл повторно (создать/обновить/пропустить). Связано с duplicate detection.
* [x] Восстановление из корзины: UI-кнопка «Восстановить» для записей со `status="deleted"`. Удаление уже soft-delete (`src/app/api/qna/[id]/route.ts`), данные не теряются. Нужно: API-эндпоинт restore (`status="deleted"` → `active` + повторный `upsertQnaItem` в Qdrant) и кнопка в списке при активном фильтре Deleted.

### Категории / разделы FAQ

* [ ] Продумать систему разделов и подразделов FAQ
* [ ] Добавить поле "Раздел" в админку
* [ ] Поддержать иерархию вида `LLM.Кодинг`
* [ ] Добавить фильтрацию по разделам
* [ ] Добавить dropdown + text search для разделов
* [ ] Продумать свободную систему тегов для преподавателей

---

## 💡 Когда-нибудь / опционально (P3)

### AI / RAG

* [ ] Настройки чанков и векторизации — advanced-функциональность для технически подготовленных пользователей, НЕ часть MVP (осознанно отложено, см. `decisions.md`).
* [ ] OpenRouter migration
* [ ] Fallback AI providers
* [ ] Semantic duplicate detection — «умная» (по эмбеддингам) версия поиска дублей; развитие soft duplicate detection из P2.

### Product / platform

* [ ] Landing page
* [ ] Multi-tenant
* [ ] Mobile app

### Data / collaboration

* [ ] Versioning FAQ
* [ ] Conflict resolution
* [x] Развести путь к SQLite по окружениям — `DATABASE_PATH` в `.env.*.local`, файлы в `data/` (`logs-dev.db`, `logs-prod.db`, `logs-staging.db`); см. `decisions.md` 2026-07-07, `spec.md` §7.1, §7.3

---

## ✅ Сделано (свежее)

* [x] Redesign — объединение FAQ и неотвеченных в один canonical route `/admin/qna` (изначально вкладки)
* [x] Redesign — cleanup legacy: удалена `/admin/unanswered` page+route, упоминания в `spec.md`/`README.md` подровнены
* [x] Redesign — единый список с фильтром по статусу (Все / Неотвеченные / Активные / Удалённые) вместо вкладок; таблица `unanswered_queries` удалена, всё в `qna_items`
* [x] Анонимность — колонка «Пользователь» (Telegram user id) убрана из UI и экспорта; бот больше не сохраняет `userId`
* [x] Unified lifecycle `unanswered → active → deleted` (без `pending`) зафиксирован в схеме, UI и `spec.md` (детали — в `decisions.md`)
