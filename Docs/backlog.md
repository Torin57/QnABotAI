<!--
Как читать этот файл:
* [ ] Название — зачем/проблема в 1 строку. [done-when: критерий готовности]
Секции сверху вниз = по убыванию приоритета. «Сделано» внизу чистим, когда суть уехала в spec.md/decisions.md.
-->

# Backlog

## 🎯 Сейчас / следующее (P1)

* [ ] Добавить создание FAQ вручную — сейчас пары можно только импортировать (Excel/PDF/DOCX); нужна форма для ручного добавления одной пары. [done-when: на `/admin/qna` есть форма «Добавить вопрос-ответ», запись создаётся как `active` и индексируется в Qdrant]
* [ ] Добавить индикатор обработки / индексации — загрузка PDF/DOCX идёт через LLM и долгая, пользователь не видит прогресс. [done-when: при upload показывается статус «обрабатывается / готово»]
* [ ] Улучшить UX уведомлений — заменить текущие сообщения на toast-нотификации (успех/ошибка действий в админке). [done-when: действия (создание/правка/удаление/upload) дают неблокирующий toast]
* [ ] Добавить bulk actions (checkbox + select all + delete) — массовые операции над списком QnA, чтобы не удалять записи по одной. [done-when: чекбоксы в строках + «выбрать все» + массовое удаление (soft-delete)]
* [ ] Довести redesign до консистентного состояния — редизайн делался через Lovable и переносился в Next.js; цель — визуальная консистентность при сохранении привычной навигации (заказчик просил не менять навигацию). [done-when: единый визуальный стиль на `/admin/qna`, агрессивные цвета убраны]

---

## 🔒 Перед публичным запуском — безопасность (P1.5)

> **Гейт:** не включать публичный / SaaS-режим, пока этот блок не закрыт.

### Core security / trust boundaries

* [ ] Threat modeling для public SaaS режима
* [ ] Продумать trust boundaries и tenant boundaries
* [ ] Secret separation (dev/staging/prod)
* [ ] Отдельный Telegram bot для production

### Upload / file handling security

* [ ] Безопасная обработка upload файлов
* [ ] Ограничения размера файлов
* [ ] Ограничения MIME/type файлов
* [ ] Upload isolation (non-public storage)
* [ ] Валидация Excel / CSV импорта
* [ ] Ограничение количества строк / размера импортов

### Auth / abuse protection

* [ ] Admin panel hardening
* [ ] Добавить авторизацию
* [ ] Rate limiting
* [ ] Basic audit logging

### AI / tooling hygiene

* [ ] Проверить Cursor / AI tooling boundaries
* [ ] Регулярная проверка dependency vulnerabilities
* [ ] Настроить basic backup strategy
* [ ] Проверить production .env hygiene перед релизом

---

## 📋 Позже (P2)

* [ ] Поддержка картинок — пожелание заказчика: прикреплять изображения к QnA-парам (вопрос/ответ с картинкой). [done-when: к паре можно прикрепить картинку, она хранится и отдаётся ботом/в админке]
* [ ] Observability / debug panel — панель для отладки RAG-пайплайна (что нашлось в Qdrant, что выбрал «Судья»).
* [ ] Улучшение readability / accessibility — в т.ч. управление размером шрифта; общая читаемость интерфейса.
* [ ] Спеки и архитектурная документация — постепенно фиксировать архитектурные/продуктовые решения и lifecycle (цель: сохранять контекст для будущего себя и ИИ-агентов). Частично закрыто: `spec.md` + `decisions.md`.
* [ ] Добавить soft duplicate detection — повторная загрузка FAQ сейчас создаёт новые записи (дубли). Выбранный подход: НЕ блокировать и НЕ авто-обновлять, а подсвечивать дубли и оставлять решение пользователю (см. `decisions.md`). [done-when: при импорте похожие записи помечаются как возможные дубли]
* [ ] Подсветка возможных дублей — UI-часть detection: визуально выделять подозрительные пары в списке.
* [ ] Продумать UX повторной загрузки FAQ — что делать, когда заливают тот же файл повторно (создать/обновить/пропустить). Связано с duplicate detection.
* [ ] Восстановление из корзины: UI-кнопка «Восстановить» для записей со `status="deleted"`. Удаление уже soft-delete (`src/app/api/qna/[id]/route.ts`), данные не теряются. Нужно: API-эндпоинт restore (`status="deleted"` → `active` + повторный `upsertQnaItem` в Qdrant) и кнопка в списке при активном фильтре Deleted.

### Категории / разделы FAQ

* [ ] Продумать систему разделов и подразделов FAQ
* [ ] Добавить поле "Раздел" в админку
* [ ] Поддержать иерархию вида `LLM.Кодинг`
* [ ] Добавить фильтрацию по разделам
* [ ] Добавить dropdown + text search для разделов
* [ ] Продумать свободную систему тегов для преподавателей

---

## 💡 Когда-нибудь / опционально (P3)

### AI / RAG

* [ ] Настройки чанков и векторизации — advanced-функциональность для технически подготовленных пользователей, НЕ часть MVP (осознанно отложено, см. `decisions.md`).
* [ ] OpenRouter migration
* [ ] Fallback AI providers
* [ ] Semantic duplicate detection — «умная» (по эмбеддингам) версия поиска дублей; развитие soft duplicate detection из P2.

### Product / platform

* [ ] Landing page
* [ ] Multi-tenant
* [ ] Mobile app

### Data / collaboration

* [ ] Versioning FAQ
* [ ] Conflict resolution

---

## ✅ Сделано (свежее)

* [x] Redesign — объединение FAQ и неотвеченных в один canonical route `/admin/qna` (изначально вкладки)
* [x] Redesign — cleanup legacy: удалена `/admin/unanswered` page+route, упоминания в `spec.md`/`README.md` подровнены
* [x] Redesign — единый список с фильтром по статусу (Все / Неотвеченные / Активные / Удалённые) вместо вкладок; таблица `unanswered_queries` удалена, всё в `qna_items`
* [x] Анонимность — колонка «Пользователь» (Telegram user id) убрана из UI и экспорта; бот больше не сохраняет `userId`
* [x] Unified lifecycle `unanswered → active → deleted` (без `pending`) зафиксирован в схеме, UI и `spec.md` (детали — в `decisions.md`)
