# План активного захода

Этот файл — **рабочая память на время одной задачи**, а не архив.
Когда заход завершён: решения → в `decisions.md`, итоговое поведение → в `spec.md`, после чего этот файл очищается под следующий заход.

---

_Активного захода нет._

Предыдущий заход (P1.5 — безопасность перед публичным запуском) завершён 2026-07-05: все 5 пунктов закрыты. Итоги разнесены: решения → `decisions.md` (записи 2026-07-03…2026-07-05), поведение → `spec.md` (§3.2, §3.4, §5.0, §7), модель угроз → `threat-model.md`, оставшиеся хвосты → `backlog.md` P1.5 (off-site бэкап, `TEACHER_CONTACT_URL` для prod, первый запуск production, регулярный `npm audit`).
