# План активного захода

*(пусто — заход «загрузка длинного транскрипта» закрыт 2026-08-08)*

**Итог.** Причина — лимит Mistral 60 req/min на embeddings. Лечение: пачки эмбеддингов + очередь 1 req/s + терпеливые ретраи; сбой индексации не отменяет черновики. На проде после деплоя `fb992e9` и `qdrant:reindex` — 104/104 фрагмента в `qna_prod_docs`, 7/7 пар в `qna_prod`.

Решение — `Docs/decisions.md` (2026-08-08), поведение — `Docs/spec.md` §3.2. В backlog остались nginx-таймауты и дубли черновиков при повторной загрузке.
