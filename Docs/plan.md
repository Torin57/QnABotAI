# План активного захода

Свободен. Предыдущий заход «После сдачи / доработка под препода (P1.80)» закрыт 2026-07-11:

1. Редирект `/` → `/admin/qna`
2. Ссылка «Связаться с преподавателем» в настройках
3. Кнопка «Это не помогло» → неотвеченные + вердикт `not_helpful` в журнале
4. Токен Telegram-бота в настройках с hot-restart polling

Детали — в `Docs/spec.md`, `Docs/decisions.md`, `Docs/backlog.md` (§ P1.80).
