# План активного захода

Этот файл — **рабочая память на время одной задачи**, а не архив.
Когда заход завершён: решения → в `decisions.md`, итоговое поведение → в `spec.md`, после чего этот файл очищается под следующий заход.

> Завершённые планы (rename `faq → qna`, unified lifecycle + анонимность) перенесены в `decisions.md` и отражены в `spec.md`.

---

## Текущий заход: доработка продукта по бэклогу P1

**Контекст:**
Возвращение к проекту после паузы. MVP работает, git чист. Цель — пройти блок P1 небольшими итерациями: одна задача → проверка пользователем → следующая. Порядок предложен от простого к сложному и по пользе.

**Шаги (предлагаемый порядок, ожидает подтверждения):**
- [x] 1. Русификация текстов интерфейса — мелкие правки надписей (подзаголовок на `/admin/qna`, название в шапке и др.).
- [ ] 2. Ручное добавление пары вопрос-ответ — форма на `/admin/qna`, запись создаётся как `active` + индексация в Qdrant. Маленькая и самая полезная задача.
- [ ] 3. Toast-уведомления — заменить текущие сообщения об успехе/ошибке. Делается раньше остальных UI-задач, чтобы новые фичи сразу использовали toast.
- [ ] 4. Индикатор обработки при загрузке PDF/DOCX — статус «обрабатывается / готово».
- [ ] 5. Bulk actions — чекбоксы, «выбрать все», массовое soft-delete.
- [ ] 6. Лог ответов бота (успешных и NULL) — отдельная таблица-лог + просмотр на `/admin/log` («Журнал»).
- [ ] 7. Консистентность редизайна — финальная визуальная полировка админки, когда весь новый UI уже на месте.

**Открытые вопросы:**
- Подтвердить порядок шагов (или переставить).

**Прогресс:**
- Шаг 1 (русификация) выполнен: подзаголовок «Управление базой вопросов и ответов», шапка «Бот „Вопрос–Ответ“», title/description страницы, заголовок на `/`, сообщения ошибок API («Запись не найдена»).
