# План активного захода

Свободен. Предыдущий заход «Изоляция прод-папки и деплой одной командой» закрыт 2026-08-06:

1. Разобран инцидент: прод-копию молча переключило с `main` на июльский `dev`; автора установить не удалось, прод при этом всё время работал на новом коде
2. Прод-клон изолирован: удалена ветка `dev`, `remote.origin.fetch` сужен до одной `main`, ссылка `origin/dev` вычищена
3. Хук `post-checkout` получил версионируемый источник `scripts/prod-post-checkout.sh` и ставится при каждом деплое
4. Деплой оформлен как `scripts/deploy.sh` с проверками прод-клона, ветки и чистоты дерева, бэкапом до миграций и health-check роутов

Детали — в `Docs/spec.md` (§7.4), `Docs/decisions.md` (2026-08-06), `Docs/backlog.md`.

Не сделано и ждёт решения: правила Cursor для обоих проектов (чтобы агент из dev-окна не работал в прод-папке через `cd`) и лишний `data/logs-dev.db` в прод-папке — след от `npm run db:migrate` без `APP_ENV`.
