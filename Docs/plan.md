# План активного захода

Этот файл — **рабочая память на время одной задачи**, а не архив.
Когда заход завершён: решения → в `decisions.md`, итоговое поведение → в `spec.md`, после чего этот файл очищается под следующий заход.

> Завершённые планы (rename `faq → qna`, unified lifecycle + анонимность, доработка продукта по бэклогу P1) перенесены в `decisions.md` и отражены в `spec.md`.

---

## Текущий заход

Нет активного захода — весь блок P1 из `backlog.md` закрыт (см. историю решений в `decisions.md`, актуальное поведение в `spec.md`).
