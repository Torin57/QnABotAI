# План активного захода

Свободен. Заход «Генерация ответов по материалам (RAG)» закрыт 2026-08-03 — P1.85 выполнен полностью. Детали: `spec.md` §3.5, `decisions.md` 2026-08-01, `backlog.md` § P1.85.

Следующее по договорённости: ветка `main` + деплой на прод (схему обсудили: создать `main` из `dev`, PR `dev → main`, прод-папка переключается на `main`; на проде — `npm ci`, `npm run db:migrate` (миграции 0007–0008), `npm run build`, рестарт systemd).
