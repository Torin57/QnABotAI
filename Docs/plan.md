# План активного захода

Свободен. Предыдущий заход «Ветка `main` + деплой на прод» закрыт 2026-08-06:

1. Ветка `main` как прод-ветка, PR `dev → main` (#1 `48b89a7`, #2 `c898177`)
2. `npm audit fix` в dev: 4 high и moderate `undici` закрыты, `package.json` не тронут
3. Деплой на прод: остановка сервиса → бэкап → `pull` → `npm ci` → миграции `0007`–`0008` → сборка → запуск
4. Порядок деплоя и проверки после запуска описаны в `spec.md` §7.4

Детали — в `Docs/spec.md` (§7.4), `Docs/decisions.md` (2026-08-06), `Docs/backlog.md`.

По итогам ручной проверки прода в бэклог (P1) добавлена одна проблема: восстановление удалённого черновика делает его `active` вместо `draft` и индексирует неодобренный ответ в Qdrant.
